# ToTo Companion

Minimal repository bootstrap for the ToTo Companion project.

## Web

This repo includes a tiny static site at the repository root so it can be deployed to Vercel immediately.

## Deploy

Vercel can deploy this repository as a static site without a build step.

